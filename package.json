{ 
  "appId": "com.TelWare.AutoUpdateExample",
  "name": "auto-update-example",
  "version": "2.0.0",
  "description": "auto-update test",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.5.5",
    "electron-builder": "^22.4.0"
  },
  "dependencies": {
    "electron-updater": "^4.2.4"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/nperalta-tw/autoupdateexample.git"
  }
}